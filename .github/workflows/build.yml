name: Build

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Provision PlexonCore 2.0.4
        run: |
          curl -fsSL -o /tmp/PlexonCore-2.0.4.jar \
            https://github.com/ZpkDxGames/PlexonCore/releases/download/v2.0.4/PlexonCore-2.0.4.jar
          echo '61d625a717da9f46ee9231e1970d84b4c317ae12cf4090cdf7c9d39b6a1a9baf  /tmp/PlexonCore-2.0.4.jar' | sha256sum -c -
          mvn -B install:install-file \
            -Dfile=/tmp/PlexonCore-2.0.4.jar \
            -DgroupId=com.zpkdxgames \
            -DartifactId=PlexonCore \
            -Dversion=2.0.4 \
            -Dpackaging=jar \
            -DgeneratePom=true

      - name: Verify
        run: mvn -B clean verify

      - name: Verify plugin metadata
        run: |
          unzip -p target/PlexonBlacksmith-1.4.1.jar plugin.yml | grep -F "version: '1.4.1'"
          unzip -p target/PlexonBlacksmith-1.4.1.jar plugin.yml | grep -F '  - PlexonCore'

      - name: Verify Core is not shaded
        run: |
          if jar tf target/PlexonBlacksmith-1.4.1.jar | grep -q '^com/zpkdxgames/plexoncore/'; then
            echo 'PlexonCore classes were shaded into PlexonBlacksmith.' >&2
            exit 1
          fi

      - name: Diff check
        run: git diff --check

      - name: Checksums
        run: |
          cd target
          sha256sum PlexonBlacksmith-1.4.1.jar > SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: PlexonBlacksmith-1.4.1
          path: |
            target/PlexonBlacksmith-1.4.1.jar
            target/SHA256SUMS.txt
          if-no-files-found: error
